<?xml version="1.0" encoding="utf-8" ?>
<Border
	x:Class="LanguageInUse.Components.CustomPicker.DropDownButton"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Name="this"
	Padding="10"
	Stroke="Gray"
	StrokeShape="RoundRectangle 10,10,10,10">

	<Grid ColumnDefinitions="*,20" RowDefinitions="Auto">
		<Label
			x:Name="lblDropDown"
			FontFamily="{Binding TextFontFamily, Source={x:Reference this}}"
			FontSize="{Binding TextSize, Source={x:Reference this}}"
			Text="{Binding Placeholder, Source={x:Reference this}}"
			TextColor="{Binding TextColor, Source={x:Reference this}}">
			<Label.Triggers>
				<MultiTrigger TargetType="Label">
					<MultiTrigger.Conditions>
						<BindingCondition Binding="{Binding Text, Source={x:Reference this}}" Value="{Binding Placeholder, Source={x:Reference this}}" />
					</MultiTrigger.Conditions>
					<Setter Property="TextColor" Value="{Binding PlaceholderColor}" />
				</MultiTrigger>
				<DataTrigger
					Binding="{Binding Text, Source={x:Reference this}}"
					TargetType="Label"
					Value="{Binding Placeholder, Source={x:Reference this}}">
					<Setter Property="TextColor" Value="{Binding PlaceholderColor}" />
				</DataTrigger>
			</Label.Triggers>
		</Label>

		<Image
			x:Name="imgDropDown"
			Grid.Column="1"
			HeightRequest="20"
			WidthRequest="20" />

		<ActivityIndicator
			Grid.Row="0"
			Grid.Column="1"
			HeightRequest="20"
			IsRunning="True"
			IsVisible="{Binding Source={x:Reference this}, Path=IsLoading}"
			WidthRequest="20" />
	</Grid>
	<Border.GestureRecognizers>
		<TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" />
	</Border.GestureRecognizers>
</Border>